#ifndef Cloth
#define Cloth
#include <vector>
#include <string>
enum Clothings{
	armour, boots, hats, helmets, cloaks
};
std::vector<std::string> cloth = {"armour", "boots", "hats", "helmets", "cloaks"
};

#endif
